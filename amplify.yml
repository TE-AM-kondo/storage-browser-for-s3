version: 1
backend:
  phases:
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - cd cdk
        - npm install -g aws-cdk
        - CDK_OUTPUTS=$(cdk deploy --require-approval never)
        - cd ..
        - CDK_OUTPUTS="$CDK_OUTPUTS" npx tsx scripts/generateAmplifyConfig.ts
frontend:
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
      - node_modules/**/*